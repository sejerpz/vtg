#labels Phase-Deploy,Featured
#How to compile and install Vala Toys for GEdit

== Prerequisites ==

 * glib (at least 2.12.0)
 * gtk+ (at least 2.10.0)
 * gedit (at least 2.22.0)
 * gtksourcecompletion http://gtksourcecomple.sourceforge.net/ (at least 0.5.2)
 * gnome build framework (at least 2.23.90)
 * gnu readline (required only for vsc-shell)
 * vala 0.5.3

== Compiling ==

Having installed the required dependecies build,
you can compile _Vtg_ with:

{{{
./autogen.sh --prefix="your home dir"/.gnome2/gedit/plugins --datadir="your home dir"/.gnome2/gedit/plugins
make
}}}

== Installing ==

To makes _Vtg_ available to all the user in the system just do:

{{{
make install
}}}

To install is just for the current account, from the root folder of the Vtg distribution, do:

{{{
mkdir ~/.gnome2/gedit/plugins/vtg
cp -r images/ ~/.gnome2/gedit/plugins/vtg/
cp vtg/vtg.gedit-plugin ~/.gnome2/gedit/plugins/
cp vtg/.libs/libvtg.so ~/.gnome2/gedit/plugins/
}}}